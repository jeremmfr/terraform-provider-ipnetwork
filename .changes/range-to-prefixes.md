<!-- markdownlint-disable-file MD013 MD041 -->
FEATURES:

* add new functions:`range_to_prefixes(start string, end string) list of string`: convert a range of IP addresses defined by a start and end address into the minimal list of CIDR prefixes that exactly cover the range.
